# CI Execution Command

> **Placeholder** — CI integration is not yet configured. This command will be implemented once an MCP connection to the CI provider is available.

---

When CI integration is ready, this command will:
- Fetch pipeline status for the current branch or PR
- Read failing job logs
- Classify failures (environment, config, test logic, flaky)
- Recommend and apply fixes
